apiVersion: v1
kind: Template
labels:
  template: rhel8-builder
metadata:
  annotations:
    description: rhel8 container
    iconClass: rhel8
    tags: instant-app
  name: rhel8-builder
objects:
- apiVersion: v1
  kind: ImageStream
  metadata:
    annotations:
    labels:
    name: rhel8
  spec: {}
- apiVersion: v1
  kind: BuildConfig
  metadata:
    name: rhel8
  spec:
    output:
      to:
        kind: ImageStreamTag
        name: rhel8:latest
    resources: {}
    source:
      contextDir: ${REPO_CONTEXTDIR}
      git:
        ref: ${REPO_REF}
        uri: ${REPO_URL}
      type: Git
    strategy:
      dockerStrategy:
        dockerfilePath: rhel8/Dockerfile
        env:
          - name: CONTAINER_NAME
            value: rhel8
      type: Docker
    triggers:
    - type: ConfigChange
parameters:
- description: Git repository with Dockerfile and slave entrypoint.
  displayName: Repository URL
  name: REPO_URL
  value: https://github.com/CentOS-PaaS-SIG/linchpin.git
- description: The sub-directory inside the repository.
  displayName: Context Directory
  name: REPO_CONTEXTDIR
  value: config/Dockerfiles
- description: The git ref or tag to use for customization.
  displayName: Git Reference
  name: REPO_REF
  value: develop
