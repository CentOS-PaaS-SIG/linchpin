---
gce-simple:
  topology:
    topology_name: gce
    resource_groups:
      - resource_group_name: "gcloud"
        resource_group_type: "gcloud"
        resource_definitions:
          - name: "gce-simple"
            role: "gcloud_gce"
            flavor: "{{ gce_simple_flavor | default('f1-micro') }}"
            region: "{{ gce_simple_region | default('us-central1-a') }}"
            image: "{{ gce_simple_image | default('centos-7') }}"
            count: 1
  layout:
    inventory_layout:
      vars:
        hostname: __ip__
      hosts:
        example-node:
          count: 1
          host_groups:
            - example
